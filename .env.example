# Copy to .env (gitignored). `just` loads it for every recipe.
COMP=titanic
SUBMIT_MODE=csv
KAGGLE_USERNAME=
TRACKER=none
# KGAT token for the nvidia-kaggle plugin. Leave empty here; set it in your shell rc.
KAGGLE_API_TOKEN=
